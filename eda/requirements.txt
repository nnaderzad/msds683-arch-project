# Local EDA-only dependencies. NOT needed by CI: the unit tests monkeypatch the
# BigQuery loader and skip plotting (pytest.importorskip), so ci.yml stays lean.
#
# BigQuery/GCS access is via the `bq` and `gsutil` CLIs (Google Cloud SDK) with ADC
# (`gcloud auth application-default login`) — not pip-installable here.
matplotlib>=3.8
