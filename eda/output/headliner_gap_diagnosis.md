# Priced-event headliner-resolution diagnosis (MIG-3)

_Generated by `eda/diagnose_headliner_gap.py`, as of 2026-06-29T01:18:39+00:00. Re-run to refresh._

Why some **priced** events carry no headliner `artist_id` in gold (so no Trends / YouTube signal can join). Tests whether it is a fixable bridge defect or a Ticketmaster **source ceiling** (no attractions returned).

- **9,661** priced events; **4,142** resolve a headliner (**42.9%**) — the honest modelable universe.
- **5,519** unresolved. Of these, **5,515** have **no `attraction_names` at all** (TM returned none) — a source ceiling, not a bug.
- **4** have attractions but still no headliner (should be ~0 — the bridge always marks one when attractions exist).
- **Safe, no-fabrication recovery:** **498** unresolved events whose name exactly matches an artist already in `dim_artist` (attach a known headliner; never invent one for artist-less club nights / tributes).
- Unresolved by genre: Rock (1062), Jazz (756), Alternative (736), Other (656), Country (474), Dance/Electronic (296), Folk (253), Pop (243), World (211), Blues (198).

**Takeaway:** the gap is overwhelmingly a TM source ceiling. The bridge logic is sound; NULL `artist_id` + the model's `has_interest` / `has_youtube` missingness flags already handle it honestly. Any enrichment should be limited to the safe name-match set and explicitly flagged as team-derived.

