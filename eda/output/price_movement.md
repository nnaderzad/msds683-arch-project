# Ticket-price movement diagnosis (model design step 0)

_Generated by `eda/diagnose_price_movement.py`, as of 2026-06-29T04:00:25+00:00. Re-run to refresh._

Decides the forecaster architecture: if prices are **flat**, the value is anchoring each show at its real price *level* (a per-show anchor), not modeling drift. `tm_observations` is the honest observed-only history (truthful but ~9 days); `fact_event_demand` is the forward-filled gold the live model trains on (flatness and history length inflated; real day-over-day changes preserved).

## `tm_observations`

- **8,870** priced events.
- **95.6%** are perfectly **flat** (8,480 events, one price level all window); **4.4%** move (390).
- **2.2%** have a **single** priced observation (191) -> anchor falls back to that lone price.
- Distinct price levels per show: `1-1`=8,480, `2-2`=381, `3-3`=9.
- Real history length (priced snapshots): `1-1`=191, `2-3`=632, `4-7`=748, `8+`=7,299.
- Among movers, price range = `26.46` median / `51.17` p90 absolute (`0.4531` / `1.0` relative to first price).
- **Cross-show price level** (last observed): p10 `0.0` / p50 `24.96` / p90 `45.76` / p99 `92.08` / max `318.44`.
- Premium shows the current $0-92 model can't reach: **95** > $90, **16** > $150.

## `fact_event_demand`

- **9,891** priced events.
- **91.6%** are perfectly **flat** (9,064 events, one price level all window); **8.4%** move (827).
- **0.2%** have a **single** priced observation (18) -> anchor falls back to that lone price.
- Distinct price levels per show: `1-1`=9,064, `2-2`=778, `3-3`=46, `4+`=3.
- Real history length (priced snapshots): `1-1`=18, `2-3`=244, `4-7`=602, `8+`=9,027.
- Among movers, price range = `27.14` median / `48.43` p90 absolute (`0.5696` / `1.0` relative to first price).
- **Cross-show price level** (last observed): p10 `0.0` / p50 `24.95` / p90 `45.76` / p99 `91.04` / max `569.05`.
- Premium shows the current $0-92 model can't reach: **102** > $90, **17** > $150.

