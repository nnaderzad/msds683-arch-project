# Ticketmaster price-gap diagnosis (COLLECT-1 step 0)

_Generated by `eda/diagnose_price_gaps.py`, as of 2026-06-28T22:55:10+00:00, over 16 daily snapshots. Re-run to refresh._

Classifies why **priced** shows have incomplete daily price series, to size the ROI of a priority re-fetch-by-id queue (which fixes coverage gaps but not source gaps).

- **9,661** events carry a price on >=1 day (the re-fetch universe).
- **0** interior coverage-gap days — event in the feed before AND after, missing in between -> **a daily re-fetch would capture these**.
- **128** unpriced-present source-gap days — captured but Ticketmaster returned no price -> **re-fetch cannot fix**.
- **0.0%** of (coverage + source) gap days are fixable by re-fetch.
- **0** events have >=1 interior coverage gap (re-fetch helps); **17** are source-gap-only (re-fetch won't).
- Edge days (ambiguous, not counted): 12,531 leading / 0 trailing.

